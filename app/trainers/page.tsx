import { SiteHeader } from "@/components/public/site-header";import { SiteFooter } from "@/components/public/site-footer";import { prisma } from "@/lib/prisma";import { Card,CardTitle } from "@/components/ui/card";
export default async function Trainers(){ const rows=await prisma.trainerProfile.findMany({include:{user:true,schedules:{include:{discipline:true}}}}); return <><SiteHeader/><main className="mx-auto max-w-7xl p-8"><h1 className="mb-6 text-4xl font-black">مربیان ما</h1><div className="grid gap-4 md:grid-cols-3">{rows.map(t=><Card key={t.id}><div className="mb-4 h-40 rounded-2xl bg-charcoal/10"/><CardTitle>{t.user.name}</CardTitle><p>{Array.isArray(t.specialties)?t.specialties.join("، "):"مربی تخصصی"}</p><p>امتیاز {String(t.rating)}</p></Card>)}</div></main><SiteFooter/></> }
