import { SiteHeader } from "@/components/public/site-header";import { SiteFooter } from "@/components/public/site-footer";import { prisma } from "@/lib/prisma";import { Card,CardTitle } from "@/components/ui/card";import { formatCurrency } from "@/lib/utils";
export default async function Shop(){ const rows=await prisma.product.findMany(); return <><SiteHeader/><main className="mx-auto max-w-7xl p-8"><h1 className="mb-6 text-4xl font-black">فروشگاه</h1><div className="grid gap-4 md:grid-cols-4">{rows.map(p=><Card key={p.id}><div className="mb-4 h-32 rounded-2xl bg-cream"/><CardTitle>{p.name}</CardTitle><p>{formatCurrency(p.price)}</p><p>موجودی: {p.stock}</p><a href="/login" className="mt-3 inline-block rounded-xl bg-charcoal px-4 py-2 text-white">خرید / ورود</a></Card>)}</div></main><SiteFooter/></> }
