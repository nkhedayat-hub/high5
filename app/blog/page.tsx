import Link from "next/link";import { SiteHeader } from "@/components/public/site-header";import { SiteFooter } from "@/components/public/site-footer";import { prisma } from "@/lib/prisma";import { Card,CardTitle } from "@/components/ui/card";
export default async function Blog(){ const rows=await prisma.blogPost.findMany({where:{status:"PUBLISHED"}}); return <><SiteHeader/><main className="mx-auto max-w-7xl p-8"><h1 className="mb-6 text-4xl font-black">بلاگ</h1><div className="grid gap-4 md:grid-cols-3">{rows.map(p=><Card key={p.id}><CardTitle>{p.title}</CardTitle><p className="my-3 text-gray-500">{p.excerpt}</p><Link className="text-mint font-bold" href={`/blog/${p.slug}`}>مطالعه</Link></Card>)}</div></main><SiteFooter/></> }
