import { prisma } from "@/lib/prisma";import { Card,CardTitle } from "@/components/ui/card";import { Table,Td,Th } from "@/components/ui/table";import { formatCurrency } from "@/lib/utils";
export default async function Products(){ const rows=await prisma.product.findMany(); return <Card><CardTitle className="mb-4">محصولات</CardTitle><Table><thead><tr><Th>نام</Th><Th>دسته</Th><Th>قیمت</Th><Th>موجودی</Th><Th>هشدار</Th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><Td>{r.name}</Td><Td>{r.category}</Td><Td>{formatCurrency(r.price)}</Td><Td>{r.stock}</Td><Td>{r.stock<5?"کمبود موجودی":"—"}</Td></tr>)}</tbody></Table></Card> }
