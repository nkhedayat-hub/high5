import { prisma } from "@/lib/prisma";import { Card,CardTitle } from "@/components/ui/card";import { Table,Td,Th } from "@/components/ui/table";import { formatDate } from "@/lib/utils";
export default async function Leads(){ const rows=await prisma.lead.findMany({orderBy:{createdAt:"desc"}}); return <Card><CardTitle className="mb-4">لیدهای سایت</CardTitle><Table><thead><tr><Th>نام</Th><Th>موبایل</Th><Th>منبع</Th><Th>وضعیت</Th><Th>تاریخ</Th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><Td>{r.fullName}</Td><Td>{r.mobile}</Td><Td>{r.source}</Td><Td>{r.status}</Td><Td>{formatDate(r.createdAt)}</Td></tr>)}</tbody></Table></Card> }
