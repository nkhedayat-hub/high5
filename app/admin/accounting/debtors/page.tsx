import { prisma } from "@/lib/prisma";import { Card,CardTitle } from "@/components/ui/card";import { Table,Td,Th } from "@/components/ui/table";import { formatCurrency } from "@/lib/utils";
export default async function Debtors(){ const rows=await prisma.memberProfile.findMany({where:{balance:{gt:0}}}); return <Card><CardTitle className="mb-4">مشتریان بدهکار</CardTitle><Table><thead><tr><Th>عضو</Th><Th>موبایل</Th><Th>بدهی</Th><Th>یادآوری</Th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><Td>{r.firstName} {r.lastName}</Td><Td>{r.mobile}</Td><Td>{formatCurrency(r.balance)}</Td><Td>ارسال پیامک (placeholder)</Td></tr>)}</tbody></Table></Card> }
