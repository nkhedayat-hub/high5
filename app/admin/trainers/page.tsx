import Link from "next/link";import { prisma } from "@/lib/prisma";import { Card,CardTitle } from "@/components/ui/card";
export default async function Trainers(){ const rows=await prisma.trainerProfile.findMany({include:{user:true,schedules:true,feedbacks:true}}); return <div className="grid gap-4 md:grid-cols-3">{rows.map(t=><Card key={t.id}><CardTitle>{t.user.name}</CardTitle><p className="mt-2">کلاس‌ها: {t.schedules.length}</p><p>بازخورد: {t.feedbacks.length}</p><p>امتیاز: {String(t.rating)}</p><Link className="mt-4 inline-block text-mint font-bold" href={`/admin/trainers/${t.id}`}>پروفایل مربی</Link></Card>)}</div> }
